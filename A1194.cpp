#include <iostream>
#include <algorithm>
#include <vector>
#include <math.h>
#include <string>

using namespace std;
#define ll long long

int main() 
{
    freopen("input.txt", "r", stdin);
    int T, n ,x;
    cin >> T;
    for (int tc = 0; tc < T; tc++)
    {
        int num_del = 0;
        cin >> n >> x;
        cout << 2 * x << endl;
    }
}